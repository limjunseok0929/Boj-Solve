#include <iostream>

using namespace std;

int main()
{
    std::cin.tie(NULL);
    ios_base::sync_with_stdio(false);
    char character;
    std::cin >> character;
    std::cout << (int)character << std::endl;
}